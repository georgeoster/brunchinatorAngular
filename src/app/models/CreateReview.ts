

export type CreateReview = {
  placeId: string,
  userName: string,
  placeName: string,
  vicinity: string,
  burger: number | null,
  bloody: number | null,
  words: string,
  reviewDate: string,
}

